name: CI

on:
  push:
    branches: [master, main]
  pull_request:
  workflow_dispatch:

jobs:
  selftest:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ['24.x']
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
      - name: Show toolchain
        run: node --version
      - name: Install dev tooling (typescript + @types/node)
        run: npm install --no-audit --no-fund
      - name: Typecheck (tsc --noEmit)
        run: npm run typecheck
      - name: Offline self-test (no API key required)
        run: node src/cli.ts selftest
      - name: Mock end-to-end eval (no API key required)
        run: node src/cli.ts eval --all --mock --concurrency 4
